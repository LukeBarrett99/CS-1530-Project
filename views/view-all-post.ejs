<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>View Post</title>
    <link rel="stylesheet" type="text/css" href="/css"/>
  </head>
  <body>
    <div class="content">
      <div class="post-info">
        <div class="info-text">
            <% for(var i = 0; i < result.length; i++) { %> 
                <% data = {id: result[i]['id'], title: result[i]['title'], user: result[i]['user'], image: result[i]['image']}; %> 
                <p>Id: <%= data.id %></p> 
                <p>Title: <%= data.title %></p> 
                <p>User: <%= data.user %></p> 
                <p>Image: <%= data.image %></p> 
            <% } %> 
        </div>
      </div>
      <div class="image-container">
        <img src='<%= data.image %>' alt="" id="imgPreview"><br>
      </div>
    </div>
  </body>
</html>
